#ifndef _UTILS_H_
#define _UTILS_H_

#include <iostream>
#include <cstring>
#include <cstdlib>
#include <cstring>
#include <ctime>

int ArgPos(char*, int, char**);
void generateArray(int*, long, int);
void printArray(int*, long);

#endif
